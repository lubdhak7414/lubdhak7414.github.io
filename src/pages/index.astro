---
import PageLayout from '../layouts/BaseLayout.astro'
import Section from '../components/Section.astro'
import Card from '../components/Card.astro'
import ProjectCard from '../components/ProjectCard.astro'
import Label from '../components/Label.astro'
import SkillLayout from '../components/SkillLayout.astro'
import PostPreview from '@/components/blog/PostPreview.astro'

import { Image } from 'astro:assets'
import astro from '../assets/face.png'

import { getAllPosts, sortMDByDate } from '@/utils'

const languages = ['Python', 'Java', 'C++', 'Dart']
const frontend = ['JavaScript', 'HTML', 'CSS', 'React', 'Tailwind CSS', 'Bootstrap', 'Vue.js']
const backend = [
	'PHP',
	'MySQL',
	'Node.js',
	'Express.js',
	'MongoDB',
	'Django',
	'Laravel',
	'Java Spring Boot'
]
const others = ['Git', 'Linux', 'AWS', 'Nginx']
const technologies = ['WordPress', 'Astro', 'Next.js', 'Gatsby', 'GraphQL', 'Stripe', 'Firebase']

const MAX_POSTS = 10
const allPosts = await getAllPosts()
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS)
---

<PageLayout meta={{ title: 'Home' }}>
	<div class='flex w-full flex-col gap-y-10'>
		<section class='flex flex-col items-center gap-y-7'>
			<Image
				src={astro}
				alt='profile'
				class='duration-800 h-28 w-auto rounded-full bg-gradient-to-r from-[#A2DFF7] via-[#E6D6B9] to-[#FF6F61] p-2 transition-all ease-in-out'
				loading='eager'
			/>

			<div class='flex flex-col items-center gap-y-4'>
				<h1 class='text-3xl font-bold'>Safwan Usaid Lubdhak</h1>
				<div class='flex flex-wrap justify-center gap-x-7 gap-y-3'>
					<Label title='Freelancer'>
						<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' class='h-5 w-5' slot='icon'
							><path
								fill='currentColor'
								d='M4.615 20q-.69 0-1.152-.462Q3 19.075 3 18.385v-9.77q0-.69.463-1.152Q3.925 7 4.615 7H9V5.615q0-.69.463-1.152Q9.925 4 10.615 4h2.77q.69 0 1.153.463q.462.462.462 1.152V7h4.385q.69 0 1.152.463q.463.462.463 1.152v9.77q0 .69-.462 1.152q-.463.463-1.153.463zm0-1h14.77q.23 0 .423-.192q.192-.193.192-.423v-9.77q0-.23-.192-.423Q19.615 8 19.385 8H4.615q-.23 0-.423.192Q4 8.385 4 8.615v9.77q0 .23.192.423q.193.192.423.192M10 7h4V5.615q0-.23-.192-.423Q13.615 5 13.385 5h-2.77q-.23 0-.423.192q-.192.193-.192.423zM4 19V8z'
							></path></svg
						>
					</Label>
					<Label title='Bangladesh'>
						<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' class='h-5 w-5' slot='icon'
							><path
								fill='currentColor'
								d='M12.003 11.73q.668 0 1.14-.475q.472-.475.472-1.143t-.475-1.14q-.476-.472-1.143-.472t-1.14.476q-.472.475-.472 1.143t.475 1.14q.476.472 1.143.472M12 19.677q2.82-2.454 4.458-4.991q1.638-2.538 1.638-4.39q0-2.744-1.737-4.53T12 3.981q-2.621 0-4.359 1.785t-1.737 4.53q0 1.852 1.638 4.39q1.639 2.537 4.458 4.99m0 1.343q-3.525-3.117-5.31-5.814q-1.786-2.697-1.786-4.909q0-3.173 2.066-5.234Q9.037 3 12 3t5.03 2.062q2.066 2.061 2.066 5.234q0 2.212-1.785 4.909q-1.786 2.697-5.311 5.814m0-10.904'
							></path></svg
						>
					</Label>
					<Label
						title='Connect on Linkedin'
						as='a'
						href='https://www.linkedin.com/in/lubdhak7414/'
						target='_blank'
					>
						<svg
							xmlns='http://www.w3.org/2000/svg'
							viewBox='0 0 16 16'
							slot='icon'
							class='h-5 w-5 text-foreground/75'
							><path
								fill='currentColor'
								d='M0 1.146C0 .513.526 0 1.175 0h13.65C15.474 0 16 .513 16 1.146v13.708c0 .633-.526 1.146-1.175 1.146H1.175C.526 16 0 15.487 0 14.854zm4.943 12.248V6.169H2.542v7.225zm-1.2-8.212c.837 0 1.358-.554 1.358-1.248c-.015-.709-.52-1.248-1.342-1.248S2.4 3.226 2.4 3.934c0 .694.521 1.248 1.327 1.248zm4.908 8.212V9.359c0-.216.016-.432.08-.586c.173-.431.568-.878 1.232-.878c.869 0 1.216.662 1.216 1.634v3.865h2.401V9.25c0-2.22-1.184-3.252-2.764-3.252c-1.274 0-1.845.7-2.165 1.193v.025h-.016l.016-.025V6.169h-2.4c.03.678 0 7.225 0 7.225z'
							></path></svg
						>
					</Label>
				</div>
			</div>
		</section>

		<Section title='About'>
			<p class='text-muted-foreground'>
				I love turning ideas into digital experiences. Let's make something great together!
			</p>
		</Section>

		<Section title='Projects'>
			<div class='relative overflow-hidden rounded-2xl'>
				<div
					id='autoScrollContainerWrapper'
					class='relative flex overflow-hidden'
					style='border-radius: inherit;'
				>
					<button
						id='scrollLeft'
						class='absolute left-0 top-1/2 z-10 -translate-y-1/2 transform rounded-full bg-blue-500 p-3 text-white shadow-lg transition duration-300 ease-in-out hover:scale-110 hover:bg-blue-400 focus:outline-none'
						aria-label='Scroll Left'
					>
						&#8592;
					</button>

					<div
						id='autoScrollContainer'
						class='scrollable-element flex space-x-3 overflow-x-auto'
						style='scroll-behavior: smooth;'
					>
						<div class='w-full flex-shrink-0 sm:w-1/2'>
							<ProjectCard
								href='/blog/green-bangla-foundation/'
								heading='Green Bangla Foundation'
								subheading='A seo optimized website for Green Bangla Foundation.'
								imagePath='/src/assets/green-bangla-mockup.jpg'
								altText='green-bangla-foundation'
								class='w-full'
							/>
						</div>
						<div class='w-full flex-shrink-0 sm:w-1/2'>
							<ProjectCard
								href='/blog/ni-driving-test-routes/'
								heading='NI Driving Test Routes'
								subheading='A blazing fast and secure website for NI Driving Test Routes.'
								imagePath='/src/assets/driving-ni.jpg'
								altText='northern-ireland-driving-test-routes'
								class='w-full'
							/>
						</div>
						<div class='w-full flex-shrink-0 sm:w-1/2'>
							<ProjectCard
								href='/blog/seo-success-story'
								heading='SEO Optimization of shopvian'
								subheading='Enhanced visibility of a website through expert SEO strategies.'
								imagePath='/src/assets/high-seo-performance.jpg'
								altText='seo optimization of shopvian'
								class='w-full'
							/>
						</div>
						<div class='w-full flex-shrink-0 sm:w-1/2'>
							<ProjectCard
								href='/blog/recovered-hacked-wordpress-site/'
								heading='Recovered WordPress Site'
								subheading='A full recovery of a hacked WordPress site with security measures.'
								imagePath='/src/assets/hacked-wordpress.jpg'
								altText='recovered-wordpress-site'
								class='w-full'
							/>
						</div>
						<div class='w-full flex-shrink-0 sm:w-1/2'>
							<ProjectCard
								href='/blog/custom-ecommerce-site/'
								heading='Custom E-commerce Site'
								subheading='Designed and developed a custom e-commerce platform.'
								imagePath='/src/assets/custom-ecommerce.jpg'
								altText='custom-ecommerce-site'
								class='w-full'
							/>
						</div>

						<div class='w-full flex-shrink-0 sm:w-1/2'>
							<ProjectCard
								href='/blog/responsive-blog-redesign/'
								heading='Responsive Blog Redesign'
								subheading='Revamped for mobile responsiveness and modern aesthetics.'
								imagePath='/src/assets/responsive-blog.jpg'
								altText='responsive-blog-redesign'
								class='w-full'
							/>
						</div>
					</div>

					<button
						id='scrollRight'
						class='absolute right-0 top-1/2 z-10 -translate-y-1/2 transform rounded-full bg-blue-500 p-3 text-white shadow-lg transition duration-300 ease-in-out hover:scale-110 hover:bg-blue-400 focus:outline-none'
						aria-label='Scroll Right'
					>
						&#8594;
					</button>
				</div>
			</div>
		</Section>

		<style>
			#autoScrollContainerWrapper {
				position: relative;
				overflow: hidden;
			}

			#autoScrollContainer {
				display: flex;
				flex-wrap: nowrap;
				overflow-x: auto;
				scroll-behavior: smooth;
				-webkit-overflow-scrolling: touch; /* Enable smooth scrolling on iOS */
				-ms-overflow-style: none; /* Hide scrollbar in IE and Edge */
				scrollbar-width: none; /* Hide scrollbar in Firefox */
			}

			#autoScrollContainer::-webkit-scrollbar {
				display: none; /* Hide the scrollbar in webkit browsers */
			}

			#container {
				white-space: nowrap;
				overflow: scroll;
			}
			@keyframes pulse {
				0% {
					transform: scale(1);
					opacity: 1;
				}
				50% {
					transform: scale(1.1);
					opacity: 0.7;
				}
				100% {
					transform: scale(1);
					opacity: 1;
				}
			}

			#scrollRight.pulsing {
				animation: pulse 1s infinite;
			}
		</style>

		<Section title='Experience'>
			<Card
				heading='Website Developer'
				subheading='Freelanced for Various Clients'
				date='2019 - Present'
				imagePath='/src/assets/web_logo.png'
				altText='Web Development and Design'
				imageClass='h-12 w-auto md:-left-16'
			>
				<ul class='ml-4 list-disc text-muted-foreground'>
					<li>
						Designed and developed responsive WordPress sites (for rapid deployment) with a focus on
						user experience and conversion optimization.
					</li>
					<li>
						Led redesigns, improving site functionality and navigation for enhanced user engagement.
					</li>
				</ul>
			</Card>
			<Card
				heading='SEO Consultant'
				subheading='Freelanced for Various Clients'
				date='2020 - Present'
				imagePath='/src/assets/seo_logo3.png'
				altText='SEO Consultant'
				imageClass='h-12 w-auto md:-left-16'
			>
				<ul class='ml-4 list-disc text-muted-foreground'>
					<li>
						Developed and executed SEO strategies that increased organic traffic by up to 30% within
						three months.
					</li>
					<li>
						Conducted keyword research, on-page SEO optimization, and technical SEO audits to
						improve site rankings.
					</li>
					<li>
						Assisted clients in improving their website structure, ensuring better crawling and
						indexing by search engines.
					</li>
				</ul>
			</Card>
		</Section>

		<Section title='Education'>
			<Card
				heading='Brac University'
				subheading='BSc. in Computer Science and Engineering'
				date='Fall 2022 - Present'
				imagePath='/src/assets/edu_logo.png'
				altText='Brac University'
				imageClass='h-12 w-auto md:-left-16'
			>
				<ul class='ml-4 list-disc text-muted-foreground'>
					<li>
						Completed core courses including Programming Language I and II, Data Structures,
						Algorithms, Discrete Mathematics, Digital Logic Design, and Data Communications.
					</li>
				</ul>
			</Card>
		</Section>

		{
			allPostsByDate.length > 0 && (
				<Section title='Posts'>
					<ul class='flex flex-col gap-y-2'>
						{allPostsByDate.map((p) => (
							<li class='flex flex-col gap-x-2 sm:flex-row'>
								<PostPreview post={p} />
							</li>
						))}
					</ul>
				</Section>
			)
		}

		<Section title='Skills'>
			<SkillLayout title='Languages' skills={languages} />
			<SkillLayout title='Frontend' skills={frontend} />
			<SkillLayout title='Backend' skills={backend} />
			<SkillLayout title='Technologies' skills={technologies} />
			<SkillLayout title='Others' skills={others} />
		</Section>
	</div>
	<script>
		const container = document.getElementById('autoScrollContainer')
		const scrollLeftButton = document.getElementById('scrollLeft')
		const scrollRightButton = document.getElementById('scrollRight')

		let scrollInterval: string | number | NodeJS.Timeout | undefined
		let autoScrollActive = true
		let restartTimeout: string | number | NodeJS.Timeout | undefined
		let scrollDirection = 1 // 1 for right, -1 for left

		// Function to check if the device is a desktop (width > 1024px)
		function isDesktop() {
			return window.innerWidth > 1024 // Adjust the width based on your desired breakpoint
		}

		// Function to update visibility of the scroll buttons based on content width
		function updateButtonVisibility() {
			if (!container || !scrollLeftButton || !scrollRightButton) return

			const totalWidth = container.scrollWidth
			const visibleWidth = container.clientWidth

			// Show the left button only if there is content to scroll to the left
			scrollLeftButton.style.display = container.scrollLeft > 150 ? 'block' : 'none'

			// Show the right button only if there is content to scroll to the right
			scrollRightButton.style.display =
				totalWidth - container.scrollLeft - visibleWidth > 150 ? 'block' : 'none'
		}

		// Check if the container and buttons exist before initializing
		if (container && scrollLeftButton && scrollRightButton) {
			function startAutoScroll() {
				if (!autoScrollActive || !container || !isDesktop()) return // Ensure it's only running on desktop

				scrollInterval = setInterval(() => {
					if (!container) return // Ensure container exists before every operation
					const totalWidth = container.scrollWidth - container.clientWidth

					// Scroll in the current direction
					container.scrollLeft += 4 * scrollDirection

					// Check if we reached the end or the beginning and reverse direction
					if (scrollDirection === 1 && container.scrollLeft >= totalWidth) {
						scrollDirection = -1 // Reverse the direction to left
					} else if (scrollDirection === -1 && container.scrollLeft <= 0) {
						scrollDirection = 1 // Reverse the direction to right
					}
					updateButtonVisibility() // Update button visibility
				}, 30)
			}

			function stopAutoScroll() {
				clearInterval(scrollInterval)
			}

			function restartAutoScroll() {
				clearTimeout(restartTimeout)

				restartTimeout = setTimeout(() => {
					autoScrollActive = true
					startAutoScroll()
				}, 1000)
			}

			function scrollLeft() {
				if (!container) return // Ensure container exists
				container.scrollLeft -= 300
				stopAutoScroll()
				autoScrollActive = false
				restartAutoScroll()
				updateButtonVisibility() // Update button visibility
			}

			function scrollRight() {
				if (!container) return // Ensure container exists
				container.scrollLeft += 300
				stopAutoScroll()
				autoScrollActive = false
				restartAutoScroll()
				updateButtonVisibility() // Update button visibility
			}

			// Event listeners for auto-scroll control
			container.addEventListener('mouseenter', stopAutoScroll)
			container.addEventListener('mouseleave', startAutoScroll)
			scrollLeftButton.addEventListener('click', scrollLeft)
			scrollRightButton.addEventListener('click', scrollRight)

			// Start auto-scrolling when the page loads, but only on desktop
			if (isDesktop()) {
				startAutoScroll()
			}

			// Initial button visibility check
			updateButtonVisibility()

			// Add a listener for window resize to stop/start auto-scrolling based on the viewport size
			window.addEventListener('resize', () => {
				if (isDesktop()) {
					startAutoScroll()
				} else {
					stopAutoScroll()
				}
			})
		} else {
			console.error('Required elements not found!')
		}
		function handleTouchScroll() {
			// Stop the pulsing effect
			if (scrollRightButton) {
				scrollRightButton.classList.remove('pulsing')
				// Hide the right arrow button
				scrollRightButton.style.display = 'none'
			}
		}

		// Event listener to detect touch start or touch move on the container
		if (container) {
			container.addEventListener('touchstart', handleTouchScroll)
			container.addEventListener('touchmove', handleTouchScroll)
		}
		// Initial setup: add pulsing effect to the right arrow on mobile
		if (scrollRightButton) {
			if (window.innerWidth <= 1024) {
				scrollRightButton.classList.add('pulsing')
			}
		}
	</script>
</PageLayout>
